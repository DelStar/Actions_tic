name: shell commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu - latest
        steps:
            - name: checkout
              uses: actions/checkout@v3
            - name: echo string
              run: echo "Hello Dele"
            - name: Multiline script
              run: |
                node -v
                pwd
 

           